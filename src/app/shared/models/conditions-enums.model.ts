export enum PossibleLetters {
  all = 'All letters',
  vowels = 'Only vowels',
  consonants = 'Only consonants',
  select = 'Select letters',
}

export enum TextStyle {
  upper = 'Uppercase',
  lower = 'Lowercase',
  cap = 'Capitalize',
}
